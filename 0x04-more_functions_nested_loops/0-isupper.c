#include "main.h"
#include <stdio.h>
/**
 * main-checks for uppercase character
 *
 * Return:1 if it is uppercase else 0
 */
int_isupper(int c)
{
	if (c >= 'a' || c <= 'z')
	{
		Return(0);
	}
		Return(1);
}
