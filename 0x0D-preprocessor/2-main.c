#include <stdio.h>

/**
 * main - entry point
 *
 * Description: prints the file name from it's compiled form
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
